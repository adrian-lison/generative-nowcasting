# this is a template for local configuration
# modify values and save as "local_config.R"
URL_CLUSTER_USER <- "INSERT username@clusterurl"
PATH_CLUSTER_REPO <- "INSERT path to folder with code on cluster"
PATH_CLUSTER_JOBS <- "INSERT path to folder with job results on cluster"
